---
- name: Run Ubuntu Terminal Commands
  hosts: all
  tasks:
    - name: Run a command
      shell: sudo snap remove curl -y
      become: yes
      register: hosts

    - name: Display command output
      debug:
        var: hosts.stdout

  
